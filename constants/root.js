const ROOT_BANNER = document.getElementById('banner');
const ROOT_HEADER = document.getElementById('header');
const ROOT_ABOUT = document.getElementById('about');
const ROOT_CLIENTS = document.getElementById('clients');
const ROOT_FOOTER = document.getElementById('footer');
const ROOT_SERVICES = document.getElementById('services');
const ROOT_FAMILY = document.getElementById('family');
const ROOT_BLOG = document.getElementById('blog');
